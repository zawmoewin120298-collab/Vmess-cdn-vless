vpn.yourdomain.com {
    # WebSocket Traffic ကိုပဲ V2Ray Container ဆီ ပို့ပေးခြင်း
    reverse_proxy /ray v2ray:8080
    
    # ရိုးရိုးဝင်ရင် ဟန်ပြ (Camouflage) အနေနဲ့ standard site တစ်ခုဆီ ပို့ထားနိုင်ပါတယ်
    reverse_proxy http://nginx
}
